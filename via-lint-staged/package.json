{
  "name": "via-lint-staged",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Reproduces the deadlock through lint-staged + eslint + eslint-plugin-prettier (synckit/worker_threads grandchild) + typescript-eslint projectService.",
  "scripts": {
    "test": "node reproduce.mjs"
  },
  "lint-staged": {
    "*.ts": "eslint --cache --fix"
  },
  "dependencies": {
    "eslint": "^10.0.0",
    "eslint-config-prettier": "^10.1.0",
    "eslint-plugin-prettier": "^5.5.0",
    "lint-staged": "17.0.4",
    "prettier": "^3.8.0",
    "typescript": "^6.0.0",
    "typescript-eslint": "^8.60.0"
  },
  "overrides": {
    "tinyexec": "1.1.2"
  },
  "engines": {
    "node": ">=18"
  }
}
