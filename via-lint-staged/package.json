{
  "name": "via-lint-staged",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Lint-staged chain test: lint-staged + eslint with typescript-eslint projectService + eslint-plugin-prettier, stdin held open as a pipe (mimics git hook env). Does not reproduce the deadlock in this minimal env — see README.",
  "scripts": {
    "test": "node reproduce.mjs"
  },
  "dependencies": {
    "eslint": "^10.0.0",
    "eslint-config-prettier": "^10.1.0",
    "eslint-plugin-prettier": "^5.5.0",
    "jiti": "^2.4.0",
    "lint-staged": "17.0.4",
    "prettier": "^3.8.0",
    "typescript": "^6.0.0",
    "typescript-eslint": "^8.60.0"
  },
  "overrides": {
    "tinyexec": "1.1.2"
  },
  "engines": {
    "node": ">=18"
  }
}
